.venv/
__pycache__/
*.pyc
.pytest_cache/

# MLflow local tracking store and artifact store -- regenerated by running src/train_mlflow.py.
# In a real team setup these would point at a shared MLflow tracking server / S3 bucket instead
# of living on one laptop, so they are not meant to be committed.
mlflow.db
mlruns/
